import React from 'react'

import './styles.css'

const Layout = ({ title, icon }) => {

    return (
        <div className="title-page">
            {title}
        </div>
    )
}

export default Layout